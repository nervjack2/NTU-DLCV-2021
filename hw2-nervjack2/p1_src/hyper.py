class hyper():
    # Model hyperparameters
    vec_dim = 100 
    latent_dim = 64
    img_dim = 64
    # Training hyperparameters
    lr = 2e-4 
    n_epoch = 200
    batch_size = 256
    # Fine tuned or not
    fine_tuned = True
    fine_tuned_path = '../log/p1_4/'
